---
title: Mixed-role pools
description: The protocol-catalog convention for tool amortization without role coupling — list tools grouped by protocol at the spine root, request a protocol per-agent.
---

A pool's agents typically share a tool set but want **different roles**. A DAG harness might run `web_research`, `corpus_research`, `compare`, and `synth` agents in one pool — every agent sees the full tool set in its KV, but each should only use the subset that matches its role. The naive options either duplicate tool schemas across N agents (tokens wasted) or force every agent to share the same role (no mixed-role pools).

The **protocol-catalog convention** solves both problems with a single prompt-level pattern and one framework knob.

## Shared mode at a glance

The framework knob that makes mixed-role pools work is called **shared mode**. It's a binary the wrapper toggles based on whether `agentPool` (or `withSpine`) was given a `systemPrompt`:

| Mode | Activation | Spine header KV | Per-agent suffix | `extendSpine` writes to |
|---|---|---|---|---|
| **Shared** | `systemPrompt` set | `[system + tool schemas]` prefilled once at the inner spine | `[role-assign system, user-task]` — no tools | inner spine (pruned at `withSpine` exit) |
| **Non-shared** | `systemPrompt` omitted | turn separator only | `[system + tools + user]` — full prefix per spawn | warm parent (persists past pool exit) |

**Use shared mode when** the pool has mixed roles drawing from one tool set (a DAG with research + compare + synth nodes), or a long chain re-emits the same tools per step. **Avoid it when** the pool's findings need to be visible to a later, separate `useAgent` that forks the same parent — the extensions would be pruned with the inner spine. See [Continuous-context spine](/under-the-hood/continuous-context-spine) for the spine-persistence consequences and [Concurrency](/under-the-hood/concurrency#spinefmt--shared-mode) for the `SpineFmt` mechanism.

## The convention

**Catalog at root.** Pass the harness's `withSpine` a `systemPrompt` that lists the enabled Apps' protocols (their `protocol.name`, `tools`, `useWhen`), plus the tool-selection rule: *"Use only the tools for a given protocol when that protocol is requested explicitly."* Pass `tools` alongside so the chat template emits the tool schemas embedded in the same root system block. Tool schemas appear once in physical KV; every agent forking from the root inherits them via metadata-only KV prefix-share.

**Protocol assignment per-agent.** Each agent's per-spec `systemPrompt` opens with `Apply the **<protocol>** protocol.` followed by its task-specific guidance. The model reads two systems in lineage — the catalog at the root, the role assignment in the agent's own suffix — and selects tools accordingly.

```
querySpine KV
  <|im_start|>system
  You are an assistant working as part of a multi-agent workflow. You have
  access to the tools below, grouped by protocol. You should only use the
  tools for a given protocol when that particular protocol is requested
  explicitly in your task instructions.

  # Protocols

  ## web_research
  Tools: web_search, fetch_page
  Use when: gathering evidence from the open web…

  ## corpus_research
  Tools: grep, read_file, search
  Use when: investigating a local document corpus…

  ## compare
  Tools: report
  Use when: …

  ## synth
  Tools: report
  Use when: …

  # Tool selection rule
  The agent system message will tell you which protocol to apply. Use
  only that protocol's tools. The agent system message also carries an
  engineered PROCESS that dictates intra-protocol ordering. Follow that
  PROCESS, but constrained to the assigned protocol's tools.

  [tool schemas embedded by chat template]
  <|im_end|>

Agent A (forks querySpine):
  inherits querySpine prefix via metadata-only KV share
  <|im_start|>system
  Apply the **web_research** protocol.
  <task-specific guidance, PROCESS, etc.>
  <|im_end|>
  <|im_start|>user
  <task>
  <|im_end|>
  <|im_start|>assistant
```

Two systems in lineage. The model sees the catalog (which tools belong to which protocol, when each is appropriate) at the root, and the role assignment (`Apply the web_research protocol`) in the agent's own suffix. Tool dispatch is grammar-restricted to whatever the catalog at root surfaced — same set across all agents — but the per-agent role channels the model toward the correct subset.

The framework supplies the bytes-locked constants for `FRAMEWORK_INTRO`, `CATALOG_ENTRY`, `TOOL_SELECTION_RULE`, and `BOUNDARY_MARKER` (the `Apply the **<name>** protocol.` line). Apps contribute their grammar-sanitized `protocol.name` + `useWhen`; the renderer assembles the catalog. See [What is an App § the App protocol](/build-an-app/what-is-an-app) for the constants and `renderSpine` for the assembler.

## The framework knob

`withSpine` accepts `systemPrompt` and `tools`:

```typescript
import { withSpine, agentPool, dag, renderSpine, reportTool } from '@lloyal-labs/lloyal-agents';

const registry = yield* AppRegistryCtx.expect();
const apps = registry.enabled();
const tools = [...apps.flatMap(a => [...a.tools]), reportTool];
const catalog = renderSpine({ apps });

yield* withSpine(
  {
    parent: session.trunk ?? undefined,
    systemPrompt: catalog,    // ← protocol catalog at root
    tools,                    // ← tool schemas in the same root block
  },
  function* (querySpine) {
    return yield* agentPool({
      orchestrate: dag(nodes),       // mixed-role DAG (research → compare → synth)
      tools,
      parent: querySpine,
      terminal: reportTool,
    });
  },
);
```

What happens at setup:
1. `withSpine` decodes `tools` into JSON schemas once, then formats `[{ role: 'system', content: catalog }]` with `{ tools: <decoded toolsJson>, addGenerationPrompt: false, enableThinking: false }`.
2. Tokenizes the result and prefills onto the spine — the catalog text plus tool-schema tokens land in physical KV exactly once.
3. The resulting `FormatConfig` (parser, grammar, format, triggers) is stashed on the `SpineFmt` Effection context.
4. The body runs (`agentPool`, etc.). Every agent forking from the inner spine inherits the catalog+tools prefix via `Branch.forkSync`'s metadata-only KV sharing.
5. `setupAgent` probes `SpineFmt` and skips re-emitting the tool schemas in each agent's suffix — they're already in attention via prefix-share. The agent's per-spec `systemPrompt` formats as `[system, user]` (a second system in lineage) and tokenizes onto the agent's branch.

When `SpineFmt` is set, agents inherit the parser/grammar/format/triggers from the root setup — no per-spawn re-formatting. When it isn't (no `systemPrompt` on the wrapping `withSpine`), agents emit their own tools in their own suffix as before.

## When to use

- **Mixed-role parallel pools.** A DAG with multiple agent kinds drawing from one tool set. Tools amortized at root; per-agent roles preserved.
- **Tool-heavy chains.** A 5-step research chain re-emitting tool schemas per spawn wastes ~700 tokens per step. Catalog at root drops that to zero past step 1.
- **You want the model to enforce protocol discipline soft-side.** The catalog's `useWhen` lines + the tool-selection rule teach the boundary. The model can still call any tool (grammar permits all), but the role assignment pushes it toward the right subset.

## When not to use

- **Single-role pools where every spawn has the exact same role.** The protocol-catalog convention helps when roles differ; if they don't, just put the role in `agentPool({ systemPrompt })` without the catalog scoping (or skip the convention entirely — re-emitting tools per spawn is fine for small pools).
- **Hard tool gating.** This is a soft enforcement — the model can ignore the rule. If you need to *prevent* certain tool calls per agent, you need either per-agent grammar restriction (a future evolution; not currently exposed) or the `protected` + `authGuard` gate for sensitive actions (see [App security](/build-an-app/app-security)).

## Where to look

- `packages/rig/src/spine-render.ts` — `renderSpine` assembles the catalog from enabled Apps' protocols.
- `packages/rig/src/protocol.ts` — the five bytes-locked constants (`FRAMEWORK_INTRO`, `CATALOG_ENTRY`, `TOOL_SELECTION_RULE`, `BOUNDARY_MARKER`, model fleet).
- `packages/agents/src/spine.ts` — `SpineOptions.systemPrompt` / `tools` and the `SpineFmt` setting flow.
- `packages/agents/src/context.ts` — `SpineFmt` context definition.
- `packages/agents/src/agent-pool.ts` — `setupAgent`'s shared-mode branch (the path that skips per-spec tool emission when `SpineFmt` is present).

## Spine compatibility

Putting the catalog on the harness's outer `withSpine` (rather than on `agentPool` itself) is load-bearing for any harness whose synth runs *post-pool* via a separate `useAgent`. `agentPool({ systemPrompt })` would create a *nested* `withSpine` whose root is pruned at pool exit — chain extensions land on that transient inner spine, and a post-pool `useAgent({ parent: querySpine })` would fork an empty branch.

When the catalog lives on the harness's outer `withSpine`, `agentPool` runs in non-shared mode internally; `extendSpine` writes onto the outer querySpine; synth's `useAgent` forks the same root and sees the chain extensions via attention. See [Continuous-context spine](/under-the-hood/continuous-context-spine) for the spine mechanics.

For pools whose synth runs as a *node inside the same `agentPool`* (a DAG with synth as the terminal node), this distinction doesn't matter — synth's branch forks from the same spineRoot the chain extensions land on.

## See also

- [Concurrency](/under-the-hood/concurrency) — how `setupAgent` reads `SpineFmt` and the five-phase tick loop
- [Prefix sharing](/under-the-hood/continuous-context-spine) — the metadata-only KV share that makes the catalog amortization free
- [Continuous-context spine](/under-the-hood/continuous-context-spine) — why catalog placement matters for spine-via-attention patterns
- [What is an App](/build-an-app/what-is-an-app) — the App protocol whose `BOUNDARY_MARKER` + `CATALOG_ENTRY` constants are the bytes the catalog renders

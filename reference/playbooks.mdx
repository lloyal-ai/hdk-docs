---
title: Playbooks
description: A prompt convention for tool amortization without role coupling — list tools grouped by playbook at the root systemPrompt, request a playbook explicitly per-agent.
---

A pool's agents typically share a tool palette but want **different roles**. Compare's DAG runs `web_research`, `corpus_research`, `compare`, and `synth` agents in one pool — every agent sees the full tool palette in its KV, but each should only use the subset that matches its role. The naive options either duplicate tool schemas across N agents (tokens wasted) or force every agent to share the same role (no mixed-role pools).

The playbooks convention solves both problems with a single prompt-level pattern and one framework knob.

## Shared mode at a glance

The framework knob that makes the playbooks convention work is called **shared mode**. It's a binary the wrapper toggles based on whether `agentPool` (or `withSpine`) was given a `systemPrompt`:

| Mode | Activation | Spine header KV | Per-agent suffix | `extendSpine` writes to |
|---|---|---|---|---|
| **Shared** | `systemPrompt` set | `[system + tool schemas]` prefilled once at the inner spine | `[role-assign system, user-task]` — no tools | inner spine (pruned at `withSpine` exit) |
| **Non-shared** | `systemPrompt` omitted | turn separator only | `[system + tools + user]` — full prefix per spawn | warm parent (persists past pool exit) |

**Use shared mode when** the pool has mixed roles drawing from one tool palette (compare's DAG) or a long chain re-emits the same tools per step. **Avoid it when** the pool's findings need to be visible to a later, separate `useAgent` that forks the same parent — the extensions would be pruned with the inner spine. See [Continuous Context Spine](/reference/continuous-context-spine) for the spine-persistence consequences and [Concurrency](/reference/concurrency#spinefmt--shared-mode) for the `SpineFmt` mechanism.

## The convention

**Playbooks at root.** Pass the harness's `withSpine` a `systemPrompt` that lists tools grouped by playbook, plus a rule: *"Use only the tools for a given playbook when that particular playbook is requested explicitly."* Pass `tools` alongside so the chat template emits the tool schemas embedded in the same root system block. Tool schemas appear once in physical KV; every agent forking from the root inherits them via metadata-only KV prefix-share.

**Playbook assignment per-agent.** Each agent's per-spec `systemPrompt` opens with `Apply the **<playbook>** playbook.` followed by its task-specific guidance. The model reads two systems in lineage — the playbooks at root, the role assignment in the agent's own suffix — and selects tools accordingly.

```
querySpine KV
  <|im_start|>system
  You are an assistant working as part of a multi-agent workflow. You have
  access to the tools below, grouped by playbook. You should only use the
  tools for a given playbook when that particular playbook is requested
  explicitly in your task instructions.

  # Playbooks

  ## web_research
  Tools: web_search, fetch_page
  Use when: …

  ## corpus_research
  Tools: grep, read_file, search
  Use when: …

  ## compare
  Tools: report
  Use when: …

  ## synth
  Tools: report
  Use when: …

  # Tool selection rule
  …

  Examples of GOOD tool selection:
  - System: "Apply the web_research playbook. Subject: …"
    Tool sequence: web_search → fetch_page → report

  Examples of BAD tool selection:
  - System: "Apply the corpus_research playbook. …"
    WRONG: assistant calls web_search.
    (web_search belongs to web_research, not corpus_research.)

  [tool schemas embedded by chat template]
  <|im_end|>

Agent A (forks querySpine):
  inherits querySpine prefix via metadata-only KV share
  <|im_start|>system
  Apply the **web_research** playbook.
  <task-specific guidance, PROCESS, etc.>
  <|im_end|>
  <|im_start|>user
  <task>
  <|im_end|>
  <|im_start|>assistant
```

Two systems in lineage. The model sees the playbooks (which tools belong to which playbook, when each is appropriate) at the root, and the role assignment (`Apply the web_research playbook`) in the agent's own suffix. Tool dispatch is grammar-restricted to whatever the playbooks at root surfaced — same set across all agents — but the per-agent role channels the model toward the correct subset.

## The framework knob

`withSpine` accepts `systemPrompt` and `tools`:

```typescript
import { withSpine, agentPool, parallel } from '@lloyal-labs/lloyal-agents';

const tools = [...sources.flatMap(s => s.tools), reportTool];

const playbooks = renderTemplate(PLAYBOOKS_TEMPLATE, { /* hasWeb, hasCorpus, ... */ });

yield* withSpine(
  {
    parent: session.trunk ?? undefined,
    systemPrompt: playbooks,            // ← playbooks at root
    tools,       // ← tool schemas in the same root block
  },
  function* (querySpine) {
    return yield* agentPool({
      orchestrate: dag(nodes),          // mixed-role DAG (research → compare → synth)
      tools,
      parent: querySpine,
      terminalToolName: 'report',
    });
  },
);
```

What happens at setup:
1. `withSpine` decodes `tools` into JSON schemas once, then formats `[{ role: 'system', content: playbooks }]` with `{ tools: <decoded toolsJson>, addGenerationPrompt: false, enableThinking: false }`.
2. Tokenizes the result and prefills onto the spine — the playbooks text plus tool-schema tokens land in physical KV exactly once.
3. The resulting `FormatConfig` (parser, grammar, format, triggers) is stashed on the `SpineFmt` Effection context.
4. The body runs (`agentPool`, etc.). Every agent forking from the inner spine inherits the playbooks+tools prefix via `Branch.forkSync`'s metadata-only KV sharing.
5. `setupAgent` probes `SpineFmt` and skips re-emitting the tool schemas in each agent's suffix — they're already in attention via prefix-share. The agent's per-spec `systemPrompt` formats as `[system, user]` (a second system in lineage) and tokenizes onto the agent's branch.

When `SpineFmt` is set, agents inherit the parser/grammar/format/triggers from the root setup — no per-spawn re-formatting. When it isn't (no `systemPrompt` on the wrapping `withSpine`), agents emit their own tools in their own suffix as before.

## When to use

- **Mixed-role parallel pools.** Compare's DAG, any harness with multiple agent kinds drawing from one tool palette. Tools amortized at root; per-agent roles preserved.
- **Tool-heavy chains.** A 5-step research chain re-emitting tool schemas per spawn wastes ~700 tokens per step. Playbooks at root drop that to zero past step 1.
- **You want the model to enforce playbook discipline soft-side.** The playbooks' GOOD/BAD examples teach the boundary. The model can still call any tool (grammar permits all), but the role assignment + examples push it toward the right subset.

## When not to use

- **Single-role pools where every spawn has the exact same role.** The playbooks convention helps when roles differ; if they don't, just put the role in `agentPool({ systemPrompt })` without the playbook scoping (or skip the convention entirely — re-emitting tools per spawn is fine for small pools).
- **Hard tool gating.** This is a soft enforcement — the model can ignore the rule. If you need to *prevent* certain tool calls per agent, you need per-agent grammar restriction (a future evolution; not currently exposed).

## Where to look

- `examples/compare/prompts/playbooks.eta` — concrete playbooks file with four entries (`web_research`, `corpus_research`, `compare`, `synth`) and literal few-shot transcripts.
- `examples/compare/harness.ts` — see how `PLAYBOOKS` and `tools` are passed to `withSpine`.
- `packages/agents/src/spine.ts` — `SpineOptions.systemPrompt` / `tools` and the `SpineFmt` setting flow.
- `packages/agents/src/context.ts` — `SpineFmt` context definition.
- `packages/agents/src/agent-pool.ts` — `setupAgent`'s shared-mode branch (the path that skips per-spec tool emission when `SpineFmt` is present).

## Worked example: compare's playbooks

Compare runs four roles in one pool — two research workers (web + corpus), three compare-axis workers, one synth. The playbooks at root teach the model the playbook→tools mapping; each per-role prompt prepends `Apply the <playbook> playbook.`:

```
prompts/research-web.eta:1     Apply the **web_research** playbook.
prompts/research-corpus.eta:1  Apply the **corpus_research** playbook.
prompts/compare.eta:1          Apply the **compare** playbook.
prompts/synthesize.eta:1       Apply the **synth** playbook.
```

The playbooks file opens:

```
You are an assistant working as part of a multi-agent workflow. You have
access to the tools below, grouped by playbook. You should only use the
tools for a given playbook when that particular playbook is requested
explicitly in your task instructions.
```

Then lists the four playbooks, the rule, and concrete few-shot transcripts (GOOD: `web_research` agent calls `web_search → fetch_page → report`; BAD: `corpus_research` agent calls `web_search` — wrong playbook). The compare and synth agents have only `report` in their playbook, so they correctly avoid retrieval — synthesizing from the prior research turns already in conversation history.

## Spine compatibility

Putting the playbooks on the harness's outer `withSpine` (rather than on `agentPool` itself) is load-bearing for any harness whose synth runs *post-pool* via a separate `useAgent`. `agentPool({ systemPrompt })` would create a *nested* `withSpine` whose root is pruned at pool exit — chain extensions land on that transient inner spine, and a post-pool `useAgent({ parent: querySpine })` would fork an empty branch.

When the playbooks live on the harness's outer `withSpine`, `agentPool` runs in non-shared mode internally; `extendSpine` writes onto the outer querySpine; synth's `useAgent` forks the same root and sees the chain extensions via attention. See [Continuous Context Spine](/reference/continuous-context-spine) for the spine mechanics.

For pools whose synth runs as a *node inside the same `agentPool`* (compare's DAG), this distinction doesn't matter — synth's branch forks from the same spineRoot the chain extensions land on.

## See also

- [Concurrency](/reference/concurrency) — how `setupAgent` reads `SpineFmt` and the five-phase tick loop
- [Prefix Sharing](/reference/prefix-sharing) — the metadata-only KV share that makes the playbook amortization free
- [Continuous Context Spine](/reference/continuous-context-spine) — why playbook placement matters for spine-via-attention patterns
- [RIG Pipeline](/reference/rig/pipeline) — reference architecture using this convention
- [Compare](/examples/compare) — worked example with the playbooks over a 6-node DAG
